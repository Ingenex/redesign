{
// Configure your Run and Preview buttons here.

// Run button configuration
  "commands": {
        "Node version": "node --version",
      "Install wordpress":"wget http://wordpress.org/latest.tar.gz && tar -xzvf latest.tar.gz && parts install php5 mysql && parts start apache2 mysql && mysqladmin -u root password 'password' && mysql -u root -p"
  },

// Preview button configuration
  "preview": {
        "Project Index (static)": "https://{{domain}}/{{index}}",
        "Current File (static)": "https://{{domain}}/{{filepath}}",
        "Box URL": "http://{{domain}}:3000/",
        "Box URL SSL": "https://{{domain}}:9500/"
  }
}


// CREATE DATABASE wordpress;CREATE USER snaper9@localhost; SET PASSWORD FOR snaper9@localhost= PASSWORD(jyd9420); GRANT ALL PRIVILEGES ON wordpress.* TO snaper9@localhost IDENTIFIED BY 'jyd9420'; FLUSH PRIVILEGES;